class Solution {
    intersectPoint(head1, head2) {
        //  code here
        let t1 = head1;
        let t2 = head2;
        
        while (t1 != t2) {
            t1 = !t1 ? head2 : t1.next;
            t2 = !t2 ? head1 : t2.next;
        }
        
        return t1;
    }
}
